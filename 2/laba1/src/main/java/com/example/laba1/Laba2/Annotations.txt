    /// аннотация @Autowired применяется к полям, методам и конструкторам
    /// Впрыскивает объект зависимости неявно

    /// Аннотация @Qualifier используется вместе с autowired
    /// в случае дубляжа нескольких объектов, используется для уточнения

    /// Аннотация @Value позволяет указать дефолтное значение
    /// Для какого-либо поля или параметра

    /// Аннотация @Scope отвечает за количество созданных объектов, т.е.
    /// Либо будет создан 1 объект и все последующие объекты класса будут просто ссылаться на первый
    /// либо по каждому вызову будет создаваться новый объект (singleton, prototype, etc.)

    /// Аннотация @PostConstruct указывается на методах, которые
    /// необходимы в выполнении после запуска конструктора (например, init)

    /// Аннотация @PreDestroy указывается на методах, которые
    /// необходимы в выполнении перед удалением бина (например, destroy)